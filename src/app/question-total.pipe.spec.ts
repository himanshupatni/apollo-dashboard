import { QuestionTotalPipe } from './question-total.pipe';

describe('QuestionTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new QuestionTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
